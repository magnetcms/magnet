# NPM Registry Configuration
registry=https://registry.npmjs.org/

# Publishing Configuration
access=public

# Save exact versions
save-exact=true

# Hoist packages
hoist=true

# Engine strict
engine-strict=true
